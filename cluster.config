{
    "__default__" :
    {
        "q"      : "long_fed25",
        "N"      : "{wildcards}.{rule}",
	"o"      : "logs/cluster/{rule}/{wildcards}.log",
	"e"      : "logs/cluster/{rule}/{wildcards}.log"
    }
}
